# Dev mirrors the production build: the default pairing command uses the public
# origin (injected at runtime, or window.location.origin); the `.lzcapp` address
# below is only surfaced as the on-box (same LightOS device) option.
VITE_CUMORA_COMPUTER_SERVER=http://cumora.cloud.lazycat.app.cumora.dev.lzcapp:5181
